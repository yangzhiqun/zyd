<%= form_for(@mytest) do |f| %>
  <% if @mytest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mytest.errors.count, "error") %> prohibited this mytest from being saved:</h2>

      <ul>
      <% @mytest.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :age %><br />
    <%= f.text_field :age %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
