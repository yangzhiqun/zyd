<% if current_user.enabled_at.nil? and current_user.apply_refused_at.nil? %>
<p class="text-warning">您的账号正在审核中，请等待审核结果</p>
<% end %>

<% if current_user.enabled_at.nil? and current_user.apply_refused_at.present? %>
    <p class="text-danger">您的账号未通过审核。</p>
<% end %>

<% if current_user.enabled_at.present? and current_user.apply_refused_at.nil? %>
    <p class="text-success">您的账号通过审核。<a href="/">回首页</a></p>
<% end %>
